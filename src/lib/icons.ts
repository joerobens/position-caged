/**
 * Icon sizes, as three steps rather than the seven arbitrary values this
 * accumulated. Anything inline with text is sm, anything standing on its own
 * in a control is md, and lg is for the one control you hit without looking.
 */
export const ICON = {
  sm: 15,
  md: 17,
  lg: 26,
} as const;
